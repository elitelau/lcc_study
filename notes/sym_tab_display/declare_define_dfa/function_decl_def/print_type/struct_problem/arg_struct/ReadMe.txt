wants_argb=0

// type of `get_average_score' is following:
// callee=(PTR->STRUCT:structarg=1), caller=(PTR->STRUCT:structarg=1)
FUNCTION(rty=FLOAT, proto=(STRUCT))

// type of `get_average_score_2' is following:
// callee=(PTR->STRUCT:structarg=1), caller=(PTR->STRUCT:structarg=1)
FUNCTION(oldstyle=1, rty=FLOAT, proto=(STRUCT))
